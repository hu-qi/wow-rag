# Backend runtime dependencies for wow-rag.
#
# These ranges intentionally pin major versions to reduce breakage when users
# reinstall the tutorial environment later. They are still broad enough for
# patch/minor security fixes.

# Web service
fastapi>=0.110,<1
uvicorn>=0.27,<1
python-dotenv>=1,<2
requests>=2,<3
pydantic>=2,<3

# OpenAI-compatible model client
openai>=1,<2

# LlamaIndex split packages used by backend/engine.py
llama-index-core>=0.10,<0.13
llama-index-embeddings-zhipuai>=0.1,<0.3
llama-index-llms-zhipuai>=0.1,<0.3
llama-index-readers-file>=0.1,<0.3
llama-index-vector-stores-faiss>=0.1,<0.3
llama-index-vector-stores-qdrant>=0.2,<0.5
llamaindex-py-client>=0.1,<0.2

# Vector stores and numeric stack
qdrant-client>=1.7,<2
faiss-cpu>=1.7,<2
scikit-learn>=1.3,<2
scipy>=1.10,<2
